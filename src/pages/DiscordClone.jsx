const DiscordClone = () => {
  return (
    <div className="flex-1 overflow-hidden">
      {/* Content will be rendered through the Layout component */}
    </div>
  );
};

export default DiscordClone;